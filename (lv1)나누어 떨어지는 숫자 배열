def solution(arr, divisor):
    answer = []
    cnt = 0
    for i in range(len(arr)):
        if arr[i]%divisor==0:
            answer.append(arr[i])
            cnt+=1
    
    if cnt==0:
        answer.append(-1)
    
    answer.sort()
    return answer
